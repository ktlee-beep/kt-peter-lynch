# 저평가 종목 선점 시스템 — 통합 고도화 로드맵

작성일: 2026-08-28
목표: **저평가 상태의 실적 우량주를 발굴하고, 시장이 인식하기 전에 선점한다.**
근거: 박세익(체슬리투자자문 대표) 2026-08-27 교보증권 머니10 인터뷰 + 윌리엄 오닐 CAN SLIM 방법론

---

## 0. 설계 원리 — 왜 2축 매트릭스인가

박세익과 캔슬림은 **진입 시점이 정반대**다. 이 대립이 설계의 핵심이다.

| | 박세익 | 캔슬림(오닐) |
|---|---|---|
| 진입 | 폭락·소외 구간에서 분할매수 | 베이스 돌파 신고가에서 매수 |
| 대상 | 실적 좋은데 **주가만 빠진** 종목 | 실적 좋고 **이미 오르는** 종목 |
| 손절 | 명시적 규칙 없음, 물타기 | -7~8% 기계적 |
| 시장판단 | 최우선 (한국 시장은 얇아서 시황 나쁘면 -70%) | M 요소로 반영하나 기준 모호 |

박세익 본인이 영상에서 캔슬림을 극찬하면서도 **M(Market Direction) 요소만 콕 집어 "판단 기준이 애매하다"**고 지적했다. 그리고 그 빈자리를 자신의 시황 분석으로 채운다.

**따라서 두 스코어를 대립축으로 배치하면 "선점"이 정의된다.**

```
                        리버모어 스코어 (모멘텀 = 캔슬림 기술적 파트)
                          낮음                    높음
                    ┌──────────────────┬──────────────────┐
       박세익   높음 │  ★ 선점 구간     │  캔슬림 구간      │
       스코어        │  (목표 영역)     │  (돌파 추격)      │
       (실적+   ────┼──────────────────┼──────────────────┤
        소외)   낮음 │  제외            │  스토리주 (경고)  │
                    └──────────────────┴──────────────────┘
```

- **선점 구간**: 실적은 3년 연속 성장인데 주가는 소외 → 매집 대상
- **선점 → 캔슬림 구간으로 이동하는 순간** = 시장이 인식하기 시작한 시점 = **매수 트리거**
- **스토리주**: 박세익이 "기획부동산 같은 주식, 절대 사지 마라"고 경고한 영역. 3년 내 적자 1회라도 있으면 즉시 배제

중요한 것은 **기존 `calcLivermoreScore`가 이미 캔슬림의 기술적 파트와 사실상 동일**하다는 점이다. 52주 고점 근접 +25, 거래량 급증 +20, 정배열 +30 — 오닐의 N·L 요소다. 즉 우측 축은 이미 구현돼 있고, **좌측 축(박세익 스코어)만 신설하면 매트릭스가 완성된다.**

---

## Phase 0 — 신뢰성 복구 (선행 필수, 반나절)

지금 스크리너 결과를 신뢰할 수 없다. 아래를 먼저 고치지 않으면 이후 모든 스코어가 오염된다.

| # | 문제 | 위치 | 수정 | 리스크 |
|---|---|---|---|---|
| 1 | 52주 고점이 실제로는 6개월 고점 | `cron.js` `analyzeStockLean` — Naver `count=120`, `closes.slice(-125)` | `count=260`, `slice(-250)` | R2 |
| 2 | `/api/52w?direction=low` 항상 빈 결과 | `analyzeStockLean`이 `near52wLow`·`high52w`·`low52w` 미기록 | 반환 객체 `a`에 4개 필드 추가 (`analysis_json`에 자동 포함) | R1 |
| 3 | DART CFS 중복 fetch | `data.js:99`와 `:109`에서 동일 호출 | 중복 제거 → **DART 호출량 절반** | R2 |
| 4 | 03 저장소 keepalive 미이관 | `03/.github/workflows/keepalive.yml` | 02로 이식 (14분 핑, 콜드스타트 제거) | R1 |

`near52wLow` 미기록은 단순 누락이 아니라 **"주가만 빠진 종목 찾기"라는 목표 기능이 통째로 죽어 있다**는 뜻이다. 최우선.

---

## Phase 1 — 3년 재무 시계열 영속화 (최대 ROI, 1~2일)

**신규 API 연동 불필요.** `data.js:86 fetchDartMultiYear()`가 이미 5개년 `{year, revenue, operatingProfit, netIncome, equity, debt, roe, debtRatio, currentRatio, opMargin}`를 반환한다. 저장만 하면 된다.

### 저장 방식
프로덕션에 DDL 권한이 없으므로 **기존 KV 패턴을 재사용**한다 (`db.js:466` `kvGet`/`kvSet`, `kt_fundamentals_cache`를 네임스페이스 키로 KV처럼 사용 중).

```
code = '__multiyear__<종목코드>'  →  raw_json = fetchDartMultiYear() 결과 배열
TTL = 365일 (연간 사업보고서는 1년에 한 번 갱신)
```

DDL 권한 확보 시 `kt_financials_annual(code, year, revenue, operating_profit, net_income, equity, debt, roe, debt_ratio, current_ratio, op_margin)` 정식 테이블로 승격.

### 배치 설계
- 월 1회 (연간 재무는 연 1회 변경)
- 호출량: 종목당 5 calls (Phase 0-3 중복 제거 후), 199종목 = **1,000 calls**. DART 일 한도 20,000 대비 5%
- 유니버스 800종목 확장 시에도 4,000 calls로 여유

### 이 한 번의 작업으로 동시 해결되는 것

| 해결 대상 | 방법 |
|---|---|
| 박세익 3대 기준 | `revenue`/`operatingProfit` 연속 증가, `netIncome` 전기간 양수 판정 |
| PEG dead code | `per / netIncome 3년 CAGR`로 자체 계산 → `calcLynchScore` PEG 브랜치(+25) 활성화 |
| Piotroski 7/10 → 10/10 | `debtRatio`·`currentRatio`가 연도별로 들어있음 (`roa`는 `netIncome/자산총계`로 파생) |
| `debt_max` 필터 무동작 | `debtRatio` 확보 |

**즉 Phase 1은 신규 기능 하나와 기존 버그 3개를 동시에 정리한다. 여기부터 시작할 것.**

---

## Phase 2 — 유니버스 확장 (선점의 전제조건, 2~3일)

**이것을 하지 않으면 "선점"은 원리적으로 불가능하다.**

현재: `KS_UNIVERSE(153) + KQ_UNIVERSE(46) = 199` 하드코딩 대형주.
문제: 박세익 논지의 대상인 중소형 실적주가 유니버스에 없다. 199개는 전부 이미 모두가 보는 종목이다.

### 목표
시가총액 상위 500~800 또는 일평균 거래대금 기준 필터링. `kt_stocks` 시딩 방식을 하드코딩 배열에서 **동적 유니버스 구성**으로 전환.

### 부하 계산
| 항목 | 800종목 기준 | 판정 |
|---|---|---|
| DART 월배치 | 4,000 calls (일한도 20,000) | 여유 |
| 일일 스캔 (일봉) | 800 calls, 30개 청크 × 500ms ≈ 27초 | 가능 |
| 일일 스캔 (수급 추가) | +800 calls ≈ 총 55초 | 가능 |

기존 `CHUNK_SIZE=30` + `setTimeout(500ms)` 페이싱을 유지하면 무리 없다. 단 Render 무료 티어 타임아웃을 확인할 것.

### 유의
유니버스를 늘리면 스크리너 결과 수가 급증한다. Phase 3의 스코어 컷오프를 먼저 정의한 뒤 확장하는 것이 안전하다.

---

## Phase 3 — 박세익 스코어 신설 (2일)

`analysis.js`에 `calcParkScore(multiYear, fundamentals, priceCtx)` 추가. 반환 `{score 0..100, grade, reasons[]}`.

### 배점 (초안 — 백테스트로 조정 필요)

| 조건 | 배점 | 근거 (영상 발언) |
|---|---|---|
| 매출 3년 연속 증가 | +25 | "매출 3년 연속 계속 증가" |
| 영업이익 3년 연속 증가 | +25 | "이익도 계속 연속 증가" |
| 3년 무적자 | +20 | — |
| **3년 내 적자 1회 이상** | **즉시 0점 (하드 배제)** | "최근 3년 동안 적자가 한 번이라도 있으면 그런 기업은 피하자" |
| 52주 고점 대비 -30% 이상 | +20 | "근데 주가만 이렇게 빠진 거" |
| 52주 고점 대비 -20~30% | +12 | — |
| PER/PBR 업종 대비 저평가 | +10 | "좋은 기업을 싸게" |

**적자 배제는 가점이 아니라 게이트로 구현할 것.** 박세익이 "스토리로만 올라가는 주식 절대 쫓아가지 마라", "기획부동산 같은 주식"이라고 가장 강하게 경고한 지점이다. 점수 합산으로 처리하면 다른 항목 고득점으로 상쇄되어 배제가 무력화된다.

### 매트릭스 판정
```
parkScore >= 60 && livermoreScore < 45   →  SEONJEOM (선점 후보)
parkScore >= 60 && livermoreScore >= 60  →  BREAKOUT (캔슬림 구간)
parkScore <  40 && livermoreScore >= 60  →  STORY_WARN (스토리주 경고)
```
`kt_daily_analysis`에 `park_score REAL`, `matrix_zone TEXT` 컬럼 추가(또는 `analysis_json`에 포함).

---

## Phase 4 — 전환 감지 = 선점의 실행 (3~4일)

발굴만으로는 선점이 아니다. **선점 구간에 있던 종목이 움직이기 시작하는 순간**을 잡아야 한다.

### 4-1. RS (시장 대비 상대강도) — 현재 완전 부재

캔슬림 L 요소의 국내 대체 지표. IBD RS Rating은 미국 전용 독점 지표이므로 자체 산출한다.

```
RS_N = (종목 N일 수익률) / (지수 N일 수익률),  N ∈ {20, 60, 120}
RS_percentile = 유니버스 내 횡단면 백분위
```

재료는 이미 있다 — `fetchIndexOHLCV(symbol, '6mo')`가 존재하고, 스캔 중 `closes` 배열이 메모리에 있다. **누락된 것은 지수 종가의 일별 영속화와 랭킹 패스뿐이다.**

### 4-2. 수급 추세 — 현재 미영속

`fetchNaverInvestor(code)`가 25일치 `{date, foreign, inst, indiv}`를 주지만 저장하지 않는다. `/api/supply-ranking`은 60종목만 30분 캐시로 재계산할 뿐이다.

신설: 야간 스캔 시 전 종목 수급 저장 → 파생 지표
- 5일 vs 20일 순매수 **가속도**
- 연속 순매수일 수
- 순매수 금액 / 시가총액 정규화 (대형주 편향 제거)

### 4-3. 선점 알림 트리거

`parkScore >= 60`인 종목에서 아래 중 2개 이상 동시 발생 시 알림:
1. RS 백분위가 하위권에서 **상승 전환** (예: 30%p 이하 → 50%p 돌파)
2. 기관 또는 외국인 **순매수 전환** (연속 순매도 종료 후 순매수 3일 이상)
3. `volRatio >= 2.0` 동반 상승

이것이 "마용성 주식이 움직이기 시작했다"의 시스템적 정의다. 기존 `alert_settings` 테이블과 `/api/alerts` 파이프라인을 재사용한다.

---

## Phase 5 — 캔슬림 정식 편입 (선택, 3일)

Phase 1·4 완료 시 CAN SLIM 7요소 중 5개가 이미 확보된다.

| 요소 | 기준 | 현 상태 |
|---|---|---|
| C 분기 EPS +18~25% | 분기 실적 | **미확보** — `reprt_code` 11013/11012/11014 추가 필요 |
| A 연간 EPS 3년 증가, ROE 17%+ | 연간 실적 | Phase 1로 확보 |
| N 신고가 돌파 | 52주 고점 | Phase 0으로 복구 |
| S 수급·부채 | 부채비율 | Phase 1로 확보 |
| L RS 80+ | 상대강도 | Phase 4로 확보 |
| I 기관 보유 증가 | 수급 추세 | Phase 4로 확보 |
| M 시장 방향 | 지수 추세 | `kt_macro_snapshots` 활용 — **자체 정의 필요** |

**M 요소가 설계 판단 지점이다.** 박세익이 "기준이 애매하다"고 지적한 바로 그 부분이며, 그는 이를 시황 분석으로 대체한다. 한국 시장은 얇아서 "실적 좋아도 시황 나쁘면 -70%"라는 것이 그의 주장이므로, M을 단순 지수 이평선 이상으로 설계할 가치가 있다 — `kt_macro_snapshots`의 `usdkrw`·`vix`·`us10y`가 이미 수집되고 있다. (영상에서 그는 환율 하락을 시스템 위기 부재의 근거로 들었다.)

---

## 실행 순서 요약

```
Phase 0 (반나절) ── 버그 5종 + keepalive 이관        [선행 필수]
      ↓
Phase 1 (1~2일) ── 3년 재무 영속화                   [최대 ROI, 버그 3개 동시 해결]
      ↓
Phase 2 (2~3일) ── 유니버스 199 → 500~800           [선점의 전제조건]
      ↓
Phase 3 (2일)   ── 박세익 스코어 + 2축 매트릭스       [발굴]
      ↓
Phase 4 (3~4일) ── RS + 수급추세 + 전환 알림         [선점]
      ↓
Phase 5 (선택)  ── 캔슬림 정식 편입
```

**Phase 1까지만 해도 즉시 쓸 수 있는 결과가 나온다** — 박세익 3대 기준 스크리닝이 가능해지고, 죽어 있던 PEG·Piotroski·부채비율 필터가 살아난다.

---

## 리스크 티어 판정 (CLAUDE.md 코드 수정 리스크 티어)

| Phase | 티어 | 근거 |
|---|---|---|
| 0 | **R2** | 로직 분기 변경 + 2개 이상 파일. `qa-logic-validator` 1개로 사후 검증 |
| 1 | **R2** | 저장 로직 추가, 계산 경로 신설. DB 스키마 미변경(KV 재사용)이므로 R3 아님 |
| 2 | **R2** | 유니버스 확장은 설정 성격이나 부하 영향 있음 |
| 3 | **R2** | 신규 스코어 함수. 재무 데이터를 읽지만 기존 계산 경로 미변경 |
| 4 | **R2** | 다중 파일. 단 `kt_supply_daily` 정식 테이블 생성 시 **R3**(DB 마이그레이션) |
| 5 | **R2** | — |

DDL을 동반하는 작업(정식 테이블 승격)은 R3 풀 파이프라인 대상이다.

---

## 검증되지 않은 가정

- **배점 수치는 초안이다.** 박세익은 영상에서 조건만 제시했을 뿐 가중치를 말한 적 없다. `runBacktest`/`runBacktestMulti`가 이미 있으므로 Phase 3 완료 후 백테스트로 조정할 것.
- **"52주 고점 대비 -30%"의 30%는 임의값이다.** 박세익은 "주가만 빠진 것"이라고만 했다.
- Render 무료 티어에서 유니버스 800 스캔이 타임아웃 없이 완주하는지 미검증.
- 캔슬림 C요소(분기 EPS)는 DART 분기보고서 연동이 추가로 필요하며 본 로드맵에 공수 미반영.

---

## 출처

- 박세익 대표이사 인터뷰, 교보증권 머니10 「(#이벤트) 삼성전자가 반포 아파트면 지금 파시겠습니까? | 체슬리투자자문 박세익 대표이사 2부 증시톡터뷰」, 2026-08-27 (23분 23초). 자막 전문 기반 인용.
- AAII, "William O'Neil's CAN SLIM Approach to Selecting Growth Stocks" — https://www.aaii.com/journal/article/william-oneil-can-slim-approach-to-selecting-growth-stocks
- 코드 근거: 본 저장소 `server.js`, `analysis.js`, `data.js`, `cron.js`, `db.js`, `schema.sql` 직접 확인 (2026-08-28)
